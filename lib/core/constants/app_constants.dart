/// App-wide constants that do not belong to a specific feature.
abstract final class AppConstants {
  /// Default group shown on first launch.
  /// TODO: Replace with user's last-selected group from SharedPreferences.
  static const defaultGroupId = 'КН-11-1';

  /// Default teacher shown on first launch.
  static const defaultTeacherId = 'борецька_і_б_';
}
